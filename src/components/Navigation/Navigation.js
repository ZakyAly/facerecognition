import React from "react";

const Navigation = () => {
  return (
    <div>
      <nav style={{ display: "flex", justifyContent: "flex-end" }}>
        <p className="f5 link dim black pa3 underline pointer">Sign Out</p>
      </nav>
    </div>
  );
};

export default Navigation;
